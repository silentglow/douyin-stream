version: "3.8"
services:
  app:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ../data:/app/data
      - ../config:/app/config
      - ../downloads:/app/downloads
    env_file:
      - ../.env
    restart: unless-stopped
